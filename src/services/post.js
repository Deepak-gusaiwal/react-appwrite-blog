import { Client, ID, Databases, Storage, Query } from "appwrite";

class PostService {
    client = new Client;
    databases;
    bucket;

    constructor(){
        // this.client = 
    }
}

const postServie = new PostService();
